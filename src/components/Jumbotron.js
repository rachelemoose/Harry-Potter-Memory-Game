import React from "react";

const Jumbotron = () => (
    <header className = "header">
    <h1>Click the Wizards!</h1>
    <h2>Click on an image to earn points, but don't click on any image more than once!</h2>
    </header>
);

export default Jumbotron;